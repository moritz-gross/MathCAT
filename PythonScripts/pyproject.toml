[project]
name = "pythonscripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["rich", "ruamel.yaml"]

[project.scripts]
audit_translations = "audit_translations.cli:main"

[dependency-groups]
dev = ["pytest"]
